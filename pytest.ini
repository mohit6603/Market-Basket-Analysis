[pytest]
; make `import preprocess` / `import mining` work without installing a package
pythonpath = src
testpaths = tests
; silence mlxtend's internal pandas-deprecation warning (their code, not ours)
filterwarnings =
    ignore::DeprecationWarning:mlxtend.*
